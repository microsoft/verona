// Copyright Microsoft and Project Verona Contributors.
// SPDX-License-Identifier: MIT

empty_declaration();

// Is this really valid?
single_arg(x);
unknown_arguments(x, y);

args_and_ret(a : U32, b : S32) : F64;

foo(a: N, b: U64 & imm): R
  where N: imm
  where R: U64 & imm
{
  let x = a + b;
  // This is just an alias to x
  let r: R = x;
  x
}

(){}
