# Example of binding with generic parameters

struct Int
struct Bool

module Outer[T1] # where   T1::+ : Int -> Int <- Int
  module Inner[T2]
    type outer_type = T1
    type inner_type = T2

module User
  use Outer[Int]::Inner[Bool]::outer_type
  use Outer[Int]::Inner[Bool]::inner_type

